server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # ── Server-Sent Events (SSE) — exact match, highest nginx priority.
    # Proxy buffering must be disabled so event frames reach the browser
    # immediately. Timeouts extended to 24h for idle SSE connections.
    location = /api/v1/events {
        proxy_pass http://nexara-api:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection '';
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
        chunked_transfer_encoding on;
        add_header X-Accel-Buffering no;
    }

    # ── General API proxy
    location /api/ {
        proxy_pass http://nexara-api:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /health/ {
        proxy_pass http://nexara-api:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri /index.html;
    }

    location = /web-health {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }
}
